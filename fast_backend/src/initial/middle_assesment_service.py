import pandas as pd
from matplotlib import pyplot as plt

from src.initial import schemas


def generate_tbse_par_affordability(df: pd.DataFrame, primitives: schemas.PrimitivesModel,
                                    population: schemas.PopulationModel, demand: schemas.DemandModel):
    # generate what is needed to the dataframe to be ok

    fig, ax = plt.subplots(1, 1)

    # Here add the plots

    #
    plt.show()


def generate_pens_parade(df: pd.DataFrame, primitives: schemas.PrimitivesModel, population: schemas.PopulationModel,
                         demand: schemas.DemandModel):
    # generate what is needed to the dataframe to be ok

    fig, ax = plt.subplots(1, 1)

    # Here add the plots

    #
    plt.show()


def generate_tbse_consumption(df: pd.DataFrame, primitives: schemas.PrimitivesModel,
                              population: schemas.PopulationModel, demand: schemas.DemandModel):
    # generate what is needed to the dataframe to be ok

    fig, ax = plt.subplots(1, 1)

    # Here add the plots

    #
    plt.show()


def generate_consumption_deviation_welfare_losses(df: pd.DataFrame, primitives: schemas.PrimitivesModel,
                                                  population: schemas.PopulationModel, demand: schemas.DemandModel):
    # generate what is needed to the dataframe to be ok

    fig, ax = plt.subplots(1, 1)

    # Here add the plots

    #
    plt.show()
